{# Image Content Block #}
<section class="section {{ block.topSpacing }} {{ block.bottomSpacing }} image-text-section">
    <div class="container {{ block.containerSize }}">
        <img class="cover" src="{{ block.image.one().getUrl() }}">
    </div>
</section>